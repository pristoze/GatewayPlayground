apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-yarp
  labels:
    app.kubernetes.io/name: gateway-yarp
    app.kubernetes.io/component: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gateway-yarp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gateway-yarp
        app.kubernetes.io/component: gateway
    spec:
      containers:
        - name: gateway-yarp
          image: gatewayplayground-gateway-yarp:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: aspnet-common-config
            - configMapRef:
                name: keycloak-auth-config
            - configMapRef:
                name: gateway-yarp-config
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-yarp
  labels:
    app.kubernetes.io/name: gateway-yarp
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gateway-yarp
  ports:
    - name: http
      port: 8080
      targetPort: http
